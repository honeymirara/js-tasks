/* Посчитать количество гласных букв (euiao) в строке. Решить через filter, reduce */

/* let str = 'klever'.split('')
let count = []

for (let i = 0; i < str.length; i++) {
    if (str[i].includes('e' || 'o' || 'e' || 'i' || 'a'))
        count.push(i)
}
console.log(count.length)
return count.length */

let str = 'klever'.split('')
let count = []

str.filter((el) => )

