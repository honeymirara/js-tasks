/* *Вычислите сумму массива целых чисел статичного массива используя рекурсию
 */
const array = [1, 2, 3, 4, 5, 6];

function doSum (array_) {
let num = array_.reduce(function(elem)) {
    return 
}
}

doSum(array_);

