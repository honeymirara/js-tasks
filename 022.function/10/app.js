// На входе число. Необходимо создать функцию, возвращающую факториал числа
// 4! = 1 * 2 * 3 * 4

const a = +prompt('введите число');

const doFuctorial = (a) => {
    let multi = 1
    for (let i = 1; i <= a; i++) {
        multi *= arr[i]
    }
}
console.log();