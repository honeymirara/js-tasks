/* У вас есть кнопка. После нажатия поменять background. */
const buttonTag = document.querySelector('button');

buttonTag.addEventListener('click', function () {

    buttonTag.style = 'background-color: pink';

});